{
  "name": "jobfinder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start:api": "json-server --watch src/database/db.json --port 3000",
    "start": "live-server public"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
